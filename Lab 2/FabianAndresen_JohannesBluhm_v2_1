function vec = func_fDarstellung(signalVec, fa, obereFreq)

    if(obereFreq > fa/2)
        obereFreq = fa/2
        fprintf("Obere Frequenz zu groß!")
    end
    
    % output fehlt
    
end

% Keine Ahnung wie man die Funktion aufrufen soll
